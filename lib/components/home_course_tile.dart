import 'package:flutter/material.dart';

class HomeCourseTile extends StatelessWidget {
  const HomeCourseTile({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}